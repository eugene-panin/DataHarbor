# Legacy pip mirror — prefer `uv sync` (see pyproject.toml / uv.lock).
# Kept for documentation only; Docker and install.sh use uv.
python-dotenv>=1.0.1
pydantic>=2.7.0
aioboto3==13.0.1
psycopg2-binary>=2.9.9
clickhouse-connect>=0.7.0
qdrant-client>=1.9.0
boto3>=1.34.0
beautifulsoup4>=4.12.3
dagster>=1.6.5
dagster-webserver>=1.6.5
dagster-postgres>=0.23.0
typer>=0.12.0
rich>=13.0.0
